import { ComponentStyleConfig } from "@chakra-ui/react";

const FormLabelStyle: ComponentStyleConfig = {
  baseStyle: {
    fontSize: "12px", // Tamaño base para el FormLabel 
    fontWeight: "bold", // Peso de la fuente 
    color: "gray.600", // Color del texto 
  },

  sizes: {},

  variants: {},

  defaultProps: {
    
  },
};

export default FormLabelStyle;
